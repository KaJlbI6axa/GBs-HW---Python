from sys import argv

file_name, worked_hour, rate, benefit = argv

calculation = (int(worked_hour) * int(rate)) + int(benefit)
print("-" * 50)
print(f"Your pay is equal [ {calculation} ]")
print("-" * 50)
# --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
a = [300, 2, 12, 44, 1, 1, 4, 10, 7, 1, 78, 123, 55]
# new = [el for el in a if el > a[a.index(el)-1]]
new = [el for num, el in enumerate(a) if a[num - 1] < a[num]]
print(new)
print("-" * 50)
# --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
print([el for el in range(20, 240) if el % 21 == 0 or el % 20 == 0])
print("-" * 50)
# --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
List = [2, 2, 2, 7, 23, 1, 44, 44, 3, 2, 10, 7, 4, 11]
print([el for el in List if List.count(el) < 2])
print("-" * 50)
# --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
''' Рофл такой  100-1000 ? xD '''
from functools import reduce
print(reduce(lambda x,y: x * y, ([el for el in range(99, 1001) if el % 2 == 0])))
print("-" * 50)
# --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
from itertools import count

for el in count(int(input('Enter the string '))):
    print(el) # внимание - беконечный цикл!

from itertools import cycle

my_list = [True, 'ABC', 123, None]
for el in cycle(my_list):
    print(el) # внимание - беконечный цикл!
print("-" * 50)
# --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
from itertools import count
from math import factorial

def fibo_gen():
    for el in count(1):
        yield factorial(el)

gen = fibo_gen()
x = 0
for i in gen:
    if x < 15:
        print(i)
        x += 1
    else:
        break 

